name: Reenvio agenda proprietaria
on:
  push:
    paths:
      - ".github/workflows/smoke-agenda-owner-temp.yml"
jobs:
  enviar:
    runs-on: ubuntu-latest
    steps:
      - name: Reenviar convite
        run: |
          set -e
          curl --fail-with-body -sS -X POST "https://hghtwlopqztfcosfxafd.supabase.co/functions/v1/cme-smoke-agenda-owner"
