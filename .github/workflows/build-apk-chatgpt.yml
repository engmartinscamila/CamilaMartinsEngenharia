name: Build temporary Android APK

on:
  push:
    branches:
      - chatgpt-apk-build
  workflow_dispatch:

jobs:
  build-apk:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    env:
      EXPO_PUBLIC_APP_ENV: homologation
      EXPO_PUBLIC_EXPECTED_SUPABASE_PROJECT_REF: nvhjcoxnzigwwbdbhkhq
      EXPO_PUBLIC_SUPABASE_URL: https://nvhjcoxnzigwwbdbhkhq.supabase.co
      EXPO_PUBLIC_SUPABASE_PUBLISHABLE_KEY: sb_publishable_DZu6VbG6f2peLeqpINAC6Q_tPmaH8yt
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'
          cache-dependency-path: portal-app/package-lock.json

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: Install dependencies
        working-directory: portal-app
        run: npm install --no-audit --no-fund

      - name: Verify environment
        working-directory: portal-app
        run: npm run check:homologation

      - name: Typecheck
        working-directory: portal-app
        run: npm run typecheck

      - name: Generate Android project
        working-directory: portal-app
        env:
          CI: '1'
        run: npx expo prebuild --platform android --clean

      - name: Build release APK
        working-directory: portal-app/android
        run: |
          chmod +x gradlew
          ./gradlew assembleRelease --no-daemon

      - name: Collect APK
        run: |
          mkdir -p artifact
          cp portal-app/android/app/build/outputs/apk/release/app-release.apk artifact/Camila-Martins-Engenharia-0.10.3.apk

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: Camila-Martins-Engenharia-APK-0.10.3
          path: artifact/Camila-Martins-Engenharia-0.10.3.apk
          if-no-files-found: error
          retention-days: 3

# Temporary build workflow; safe to delete with the chatgpt-apk-build branch.
